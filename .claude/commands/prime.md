Please read and confirm the following before we start:

1. Read `CLAUDE.md` for project context
2. Run !`docker compose ps` to check which services are currently running
3. Run !`git status` to see any uncommitted changes
4. Run !`git log --oneline -5` for recent work

Once done, give me a one-line status on:
- Which services are up/down
- Any uncommitted work in progress
